services:
  caishen-web:
    build: .
    ports:
      - "8788:8788"
    env_file:
      - .env
    environment:
      CAISHEN_HOST: 0.0.0.0
      CAISHEN_DATA_DIR: /data
    volumes:
      - ./data:/data
    restart: unless-stopped
